1. Client Layer (클라이언트 계층)
React Native + Expo:
모바일 앱을 개발하는 프레임워크입니다. React Native는 크로스 플랫폼 모바일 앱 개발을 가능하게 하고, Expo는 React Native 앱 개발을 간편하게 도와주는 도구입니다.
사용자 유형:
User (일반 사용자)
Owner (소유자)
Member (회원)
각각의 사용자들은 이 모바일 앱을 통해 서비스에 접근합니다.
Firebase:
인증, 실시간 데이터베이스, 푸시 알림 등 외부 서비스(외부 인증 및 백엔드 서비스 등)를 제공하며, 앱과 연동됩니다.
2. Infrastructure Layer (인프라 계층)
Spring Boot (초록색 아이콘):
백엔드 서버를 담당합니다. 클라이언트에서 요청을 받아 비즈니스 로직을 처리합니다.
Docker (고래 아이콘):
백엔드 서버를 컨테이너화하여 어디서든 동일한 환경에서 실행 가능하도록 합니다.
AWS Lambda (오렌지 박스):
서버리스 컴퓨팅 서비스로, 특정 이벤트에 따라 자동으로 코드를 실행하는 역할을 합니다.
S3 (빨간 상자):
AWS의 객체 저장소 서비스로, 이미지, 동영상 등 정적 파일을 저장합니다.
3. Data Layer (데이터 계층)
MySQL:
관계형 데이터베이스로, 앱과 서버에서 사용하는 데이터가 저장됩니다.
4. CI/CD Pipeline (지속적 통합/배포 파이프라인)
GitHub:
코드 저장소로 개발자들이 코드를 관리합니다.
GitHub Actions:
자동화 워크플로우를 구성해, 코드 변경 시 자동 빌드/테스트/배포를 수행합니다.
Backend Pipeline:
빌드된 백엔드 애플리케이션을 Docker 이미지로 만들고, 이를 다시 컨테이너 환경에 배포합니다.
Frontend Pipeline:
Expo를 통해 빌드된 모바일 앱을 구글 플레이 스토어에 배포합니다.
전체 흐름 요약
개발자가 GitHub에 코드를 푸시하면 GitHub Actions가 자동으로 빌드/테스트/배포 파이프라인을 실행합니다.
백엔드 파이프라인은 Docker 컨테이너로 백엔드를 배포하고, AWS Lambda와 연동됩니다.
클라이언트(React Native + Expo) 앱은 Firebase를 통해 인증 등 외부 서비스를 사용하며, 백엔드 서버와 API 통신을 합니다.
앱 사용자들은 앱을 통해 데이터를 요청하거나 저장하고, 이 데이터는 MySQL 데이터베이스에 저장됩니다.
정적 파일은 S3에 저장되어 앱과 백엔드에서 사용됩니다.
완성된 모바일 앱은 구글 플레이 스토어에 배포되어 사용자들이 다운로드할 수 있습니다.